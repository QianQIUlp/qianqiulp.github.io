# Person-first reveal screenshot evidence

Captured from the local production build on 2026-07-21 with headless Chromium.

Viewports:

- Desktop: 1440 × 1024
- Mobile: 375 × 812

States:

- `home-initial-*`: initial person-first composition before any room choice
- `home-discover-writing-*`: pointer held over the shadowed writing desk
- `home-life-*`: settled guitar/life close view
- `home-discovered-*`: overview after returning from a close view
- `blog-*`, `projects-*`, `post-*`: above-the-fold interior continuity checks

These files verify the current implementation described in root `design-qa.md`. The older
`docs/qa/screenshots/interactive-room/` directory remains historical evidence for the previous room behavior;
it is not the current art-direction target.
